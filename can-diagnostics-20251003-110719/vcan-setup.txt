+ sudo modprobe can
+ sudo modprobe can_raw
+ sudo modprobe vcan
+ ip -br link
+ grep -q '^vcan0'
+ sudo ip link add dev vcan0 type vcan
+ sudo ip link set up vcan0
+ set +x

=== vcan0 state after bring-up ===
vcan0            UNKNOWN        <NOARP,UP,LOWER_UP> 
